---
logger:
  level: "INFO"

storage:
  inmemory: true
  dsn: "host=127.0.0.1 user=calendar password=calendar dbname=calendar sslmode=disable"

http:
  host: 0.0.0.0
  port: 8080

grpc:
  host: 0.0.0.0
  port: 8081